{
  "name": "request-ish",
  "version": "0.0.0-development",
  "description": "`request`-ish interface with far fewer dependencies. Tiny footprint. Uses axios under the hood. No aftertaste.",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "scripts": {
    "test": "mocha build/test",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check",
    "travis-deploy-once": "travis-deploy-once",
    "semantic-release": "semantic-release"
  },
  "keywords": [
    "request",
    "axios"
  ],
  "author": "Justin Beckwith",
  "license": "MIT",
  "devDependencies": {
    "@types/mocha": "^5.2.1",
    "@types/nock": "^9.1.3",
    "@types/request": "^2.47.0",
    "gts": "^0.6.0",
    "mocha": "^5.2.0",
    "nock": "^9.3.2",
    "semantic-release": "^15.5.1",
    "travis-deploy-once": "^5.0.0",
    "typescript": "~2.9.0"
  },
  "dependencies": {
    "axios": "^0.18.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/JustinBeckwith/request-ish.git"
  }
}
